import React from "react";
import { StyleSheet } from "react-native";

const validationColor = "orange";

const styles = StyleSheet.create({
  input: {
    width: "100%",
    backgroundColor: "#F3F3F3",
    margin: 5,
    padding: 10,
    borderRadius: 10,
  },
});

export default styles;
